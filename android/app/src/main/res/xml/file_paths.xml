<?xml version="1.0" encoding="utf-8"?>
<!--
  The only path the app ever exposes: the folder inside its own cache where a
  shared picture is written. No external storage, no documents, no database.
-->
<paths>
    <cache-path name="share" path="share/" />
</paths>
